#include "replace.hpp"

void	err_message(std::string err)
{
	std::cout << FGRND_R_RED << "Error! " << err << RESET << std::endl;
	return (0);
}

int

int	main(int argc, char **argv)
{
	if (argc != 4)
		err_message("Wrong number of arguments");
	if ()
	return (0);
}